# Midterm_Project file (MinThantHein)

MinThantHein file is the zip file which collects the collection of Midterm_Project files! As our midterm project, we need to create a website using flask micro web framework! So, we are only able to run/ host the website locally, meaning that we need to use local host. 

For running the file locally, we need to run the following lines in terminal command prompt: 

1.pip install flask pandas scikit-learn

2.

3.


Notes:

-pip install flask pandas scikit-learn (this line has to be ran to make sure we are having the flask framework!)
-
